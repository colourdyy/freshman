sb.append(body).append(api).append(sessionId).append(timeStamp).append(channel).append(accessCode)
						.append(accessKey).append(mid);
				String authSign = Md5Utils.MD5Encode(sb.toString()).toUpperCase();
				String signature = contextMap.get("signature");
				if (!authSign.equals(signature)) {}
				
				
				str = new String(Base64.encodeBase64(crypted));
				
				
				XmlGetUserStatusRequest
				XmlGetUserStatusResult
				XmlSetUserPackageRequest
				XmlSetUserPackageResult
				
				XmlBaseResult
				XmlGetSceneCYResult
				


channel£º10
accessCode:161106036228
accessKey: 8EH12@BE8CFB5530


LocalDateTime.now().minusMonths(6).atZone(ZoneId.systemDefault()).toInstant().toEpochMilli();


Unrecognized SSL message, plaintext connection


